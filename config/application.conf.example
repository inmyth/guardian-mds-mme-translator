channel = eq
kafka-config = {
  server = "172.21.252.175:9092"
  topic = "[optional]topic_from_receiver_or_(set_or_tfex)-(dbtype)"
  batch-size = 10000
}
redis-config = {
  host = "localhost"
  port = 6379
  number = 0
  username = "[optional]username"
  password = "[optional]password"
}
filename = "[optional] dump file path"